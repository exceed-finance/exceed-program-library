/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DepositTokensInstructionAccounts = {
  authority?: Signer;
  guardian: PublicKey | Pda;
  sale: PublicKey | Pda;
  salePurchaseAta: PublicKey | Pda;
  authorityPurchaseAta: PublicKey | Pda;
  purchaseProgram: PublicKey | Pda;
};

// Data.
export type DepositTokensInstructionData = {
  discriminator: Uint8Array;
  amountToDeposit: bigint;
};

export type DepositTokensInstructionDataArgs = {
  amountToDeposit: number | bigint;
};

export function getDepositTokensInstructionDataSerializer(): Serializer<
  DepositTokensInstructionDataArgs,
  DepositTokensInstructionData
> {
  return mapSerializer<
    DepositTokensInstructionDataArgs,
    any,
    DepositTokensInstructionData
  >(
    struct<DepositTokensInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amountToDeposit', u64()],
      ],
      { description: 'DepositTokensInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([176, 83, 229, 18, 191, 143, 176, 150]),
    })
  ) as Serializer<
    DepositTokensInstructionDataArgs,
    DepositTokensInstructionData
  >;
}

// Args.
export type DepositTokensInstructionArgs = DepositTokensInstructionDataArgs;

// Instruction.
export function depositTokens(
  context: Pick<Context, 'identity' | 'programs'>,
  input: DepositTokensInstructionAccounts & DepositTokensInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'earlyPurchase',
    'EmzVeKtVHRc6AuzrJtowoJ5qfEkpK1R9WzAmcnjzgF1V'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    guardian: {
      index: 1,
      isWritable: false as boolean,
      value: input.guardian ?? null,
    },
    sale: { index: 2, isWritable: true as boolean, value: input.sale ?? null },
    salePurchaseAta: {
      index: 3,
      isWritable: true as boolean,
      value: input.salePurchaseAta ?? null,
    },
    authorityPurchaseAta: {
      index: 4,
      isWritable: true as boolean,
      value: input.authorityPurchaseAta ?? null,
    },
    purchaseProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.purchaseProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DepositTokensInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDepositTokensInstructionDataSerializer().serialize(
    resolvedArgs as DepositTokensInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
