/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type FreezeSaleInstructionAccounts = {
  authority?: Signer;
  guardian: PublicKey | Pda;
  sale: PublicKey | Pda;
};

// Data.
export type FreezeSaleInstructionData = { discriminator: Uint8Array };

export type FreezeSaleInstructionDataArgs = {};

export function getFreezeSaleInstructionDataSerializer(): Serializer<
  FreezeSaleInstructionDataArgs,
  FreezeSaleInstructionData
> {
  return mapSerializer<
    FreezeSaleInstructionDataArgs,
    any,
    FreezeSaleInstructionData
  >(
    struct<FreezeSaleInstructionData>([['discriminator', bytes({ size: 8 })]], {
      description: 'FreezeSaleInstructionData',
    }),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([234, 81, 56, 199, 204, 6, 104, 121]),
    })
  ) as Serializer<FreezeSaleInstructionDataArgs, FreezeSaleInstructionData>;
}

// Instruction.
export function freezeSale(
  context: Pick<Context, 'identity' | 'programs'>,
  input: FreezeSaleInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'earlyPurchase',
    'EmzVeKtVHRc6AuzrJtowoJ5qfEkpK1R9WzAmcnjzgF1V'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    guardian: {
      index: 1,
      isWritable: false as boolean,
      value: input.guardian ?? null,
    },
    sale: { index: 2, isWritable: true as boolean, value: input.sale ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getFreezeSaleInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
