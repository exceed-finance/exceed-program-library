/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  i64,
  mapSerializer,
  option,
  string,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeSaleInstructionAccounts = {
  admin: Signer;
  sale: PublicKey | Pda;
  purchaseMint: PublicKey | Pda;
  paymentMint: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeSaleInstructionData = {
  discriminator: Uint8Array;
  id: bigint;
  guardPurchases: boolean;
  paymentAmount: bigint;
  priceFeedIdHex: string;
  maxPriceFeedAge: bigint;
  maxTokensTotal: Option<bigint>;
  maxTokensPerUser: Option<bigint>;
  startTimestamp: Option<bigint>;
  endTimestamp: Option<bigint>;
};

export type InitializeSaleInstructionDataArgs = {
  id: number | bigint;
  guardPurchases: boolean;
  paymentAmount: number | bigint;
  priceFeedIdHex: string;
  maxPriceFeedAge: number | bigint;
  maxTokensTotal: OptionOrNullable<number | bigint>;
  maxTokensPerUser: OptionOrNullable<number | bigint>;
  startTimestamp: OptionOrNullable<number | bigint>;
  endTimestamp: OptionOrNullable<number | bigint>;
};

export function getInitializeSaleInstructionDataSerializer(): Serializer<
  InitializeSaleInstructionDataArgs,
  InitializeSaleInstructionData
> {
  return mapSerializer<
    InitializeSaleInstructionDataArgs,
    any,
    InitializeSaleInstructionData
  >(
    struct<InitializeSaleInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['id', u64()],
        ['guardPurchases', bool()],
        ['paymentAmount', u64()],
        ['priceFeedIdHex', string()],
        ['maxPriceFeedAge', u64()],
        ['maxTokensTotal', option(u64())],
        ['maxTokensPerUser', option(u64())],
        ['startTimestamp', option(i64())],
        ['endTimestamp', option(i64())],
      ],
      { description: 'InitializeSaleInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([208, 103, 34, 154, 179, 6, 125, 208]),
    })
  ) as Serializer<
    InitializeSaleInstructionDataArgs,
    InitializeSaleInstructionData
  >;
}

// Args.
export type InitializeSaleInstructionArgs = InitializeSaleInstructionDataArgs;

// Instruction.
export function initializeSale(
  context: Pick<Context, 'programs'>,
  input: InitializeSaleInstructionAccounts & InitializeSaleInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'earlyPurchase',
    'EmzVeKtVHRc6AuzrJtowoJ5qfEkpK1R9WzAmcnjzgF1V'
  );

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    sale: { index: 1, isWritable: true as boolean, value: input.sale ?? null },
    purchaseMint: {
      index: 2,
      isWritable: false as boolean,
      value: input.purchaseMint ?? null,
    },
    paymentMint: {
      index: 3,
      isWritable: false as boolean,
      value: input.paymentMint ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeSaleInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeSaleInstructionDataSerializer().serialize(
    resolvedArgs as InitializeSaleInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
