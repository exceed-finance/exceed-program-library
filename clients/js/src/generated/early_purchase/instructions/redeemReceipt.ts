/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RedeemReceiptInstructionAccounts = {
  buyer: Signer;
  sale: PublicKey | Pda;
  receipt: PublicKey | Pda;
  buyerPurchaseAta: PublicKey | Pda;
  configPurchaseAta: PublicKey | Pda;
  purchaseProgram: PublicKey | Pda;
};

// Data.
export type RedeemReceiptInstructionData = { discriminator: Uint8Array };

export type RedeemReceiptInstructionDataArgs = {};

export function getRedeemReceiptInstructionDataSerializer(): Serializer<
  RedeemReceiptInstructionDataArgs,
  RedeemReceiptInstructionData
> {
  return mapSerializer<
    RedeemReceiptInstructionDataArgs,
    any,
    RedeemReceiptInstructionData
  >(
    struct<RedeemReceiptInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'RedeemReceiptInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([213, 100, 93, 195, 251, 161, 175, 16]),
    })
  ) as Serializer<
    RedeemReceiptInstructionDataArgs,
    RedeemReceiptInstructionData
  >;
}

// Instruction.
export function redeemReceipt(
  context: Pick<Context, 'programs'>,
  input: RedeemReceiptInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'earlyPurchase',
    'EmzVeKtVHRc6AuzrJtowoJ5qfEkpK1R9WzAmcnjzgF1V'
  );

  // Accounts.
  const resolvedAccounts = {
    buyer: {
      index: 0,
      isWritable: true as boolean,
      value: input.buyer ?? null,
    },
    sale: { index: 1, isWritable: true as boolean, value: input.sale ?? null },
    receipt: {
      index: 2,
      isWritable: true as boolean,
      value: input.receipt ?? null,
    },
    buyerPurchaseAta: {
      index: 3,
      isWritable: true as boolean,
      value: input.buyerPurchaseAta ?? null,
    },
    configPurchaseAta: {
      index: 4,
      isWritable: true as boolean,
      value: input.configPurchaseAta ?? null,
    },
    purchaseProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.purchaseProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRedeemReceiptInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
