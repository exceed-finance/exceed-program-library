/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  i64,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { SaleState, SaleStateArgs, getSaleStateSerializer } from '../types';

export type Sale = Account<SaleAccountData>;

export type SaleAccountData = {
  discriminator: Uint8Array;
  id: bigint;
  bump: number;
  state: SaleState;
  creator: PublicKey;
  guardPurchases: boolean;
  maxTokensTotal: Option<bigint>;
  maxTokensPerUser: Option<bigint>;
  startTimestamp: Option<bigint>;
  endTimestamp: Option<bigint>;
  purchaseMint: PublicKey;
  purchaseProgram: PublicKey;
  paymentMint: PublicKey;
  paymentProgram: PublicKey;
  paymentAmount: bigint;
  paymentPriceFeedId: Uint8Array;
  maxPriceFeedAge: bigint;
  numTokensPurchased: bigint;
  numTokensDeposited: bigint;
  numTokensDistributed: bigint;
};

export type SaleAccountDataArgs = {
  id: number | bigint;
  bump: number;
  state: SaleStateArgs;
  creator: PublicKey;
  guardPurchases: boolean;
  maxTokensTotal: OptionOrNullable<number | bigint>;
  maxTokensPerUser: OptionOrNullable<number | bigint>;
  startTimestamp: OptionOrNullable<number | bigint>;
  endTimestamp: OptionOrNullable<number | bigint>;
  purchaseMint: PublicKey;
  purchaseProgram: PublicKey;
  paymentMint: PublicKey;
  paymentProgram: PublicKey;
  paymentAmount: number | bigint;
  paymentPriceFeedId: Uint8Array;
  maxPriceFeedAge: number | bigint;
  numTokensPurchased: number | bigint;
  numTokensDeposited: number | bigint;
  numTokensDistributed: number | bigint;
};

export function getSaleAccountDataSerializer(): Serializer<
  SaleAccountDataArgs,
  SaleAccountData
> {
  return mapSerializer<SaleAccountDataArgs, any, SaleAccountData>(
    struct<SaleAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['id', u64()],
        ['bump', u8()],
        ['state', getSaleStateSerializer()],
        ['creator', publicKeySerializer()],
        ['guardPurchases', bool()],
        ['maxTokensTotal', option(u64())],
        ['maxTokensPerUser', option(u64())],
        ['startTimestamp', option(i64())],
        ['endTimestamp', option(i64())],
        ['purchaseMint', publicKeySerializer()],
        ['purchaseProgram', publicKeySerializer()],
        ['paymentMint', publicKeySerializer()],
        ['paymentProgram', publicKeySerializer()],
        ['paymentAmount', u64()],
        ['paymentPriceFeedId', bytes({ size: 32 })],
        ['maxPriceFeedAge', u64()],
        ['numTokensPurchased', u64()],
        ['numTokensDeposited', u64()],
        ['numTokensDistributed', u64()],
      ],
      { description: 'SaleAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([202, 64, 232, 171, 178, 172, 34, 183]),
    })
  ) as Serializer<SaleAccountDataArgs, SaleAccountData>;
}

export function deserializeSale(rawAccount: RpcAccount): Sale {
  return deserializeAccount(rawAccount, getSaleAccountDataSerializer());
}

export async function fetchSale(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Sale> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'Sale');
  return deserializeSale(maybeAccount);
}

export async function safeFetchSale(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Sale | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeSale(maybeAccount) : null;
}

export async function fetchAllSale(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Sale[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'Sale');
    return deserializeSale(maybeAccount);
  });
}

export async function safeFetchAllSale(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Sale[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeSale(maybeAccount as RpcAccount));
}

export function getSaleGpaBuilder(context: Pick<Context, 'rpc' | 'programs'>) {
  const programId = context.programs.getPublicKey(
    'earlyPurchase',
    'EmzVeKtVHRc6AuzrJtowoJ5qfEkpK1R9WzAmcnjzgF1V'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      id: number | bigint;
      bump: number;
      state: SaleStateArgs;
      creator: PublicKey;
      guardPurchases: boolean;
      maxTokensTotal: OptionOrNullable<number | bigint>;
      maxTokensPerUser: OptionOrNullable<number | bigint>;
      startTimestamp: OptionOrNullable<number | bigint>;
      endTimestamp: OptionOrNullable<number | bigint>;
      purchaseMint: PublicKey;
      purchaseProgram: PublicKey;
      paymentMint: PublicKey;
      paymentProgram: PublicKey;
      paymentAmount: number | bigint;
      paymentPriceFeedId: Uint8Array;
      maxPriceFeedAge: number | bigint;
      numTokensPurchased: number | bigint;
      numTokensDeposited: number | bigint;
      numTokensDistributed: number | bigint;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      id: [8, u64()],
      bump: [16, u8()],
      state: [17, getSaleStateSerializer()],
      creator: [18, publicKeySerializer()],
      guardPurchases: [50, bool()],
      maxTokensTotal: [51, option(u64())],
      maxTokensPerUser: [null, option(u64())],
      startTimestamp: [null, option(i64())],
      endTimestamp: [null, option(i64())],
      purchaseMint: [null, publicKeySerializer()],
      purchaseProgram: [null, publicKeySerializer()],
      paymentMint: [null, publicKeySerializer()],
      paymentProgram: [null, publicKeySerializer()],
      paymentAmount: [null, u64()],
      paymentPriceFeedId: [null, bytes({ size: 32 })],
      maxPriceFeedAge: [null, u64()],
      numTokensPurchased: [null, u64()],
      numTokensDeposited: [null, u64()],
      numTokensDistributed: [null, u64()],
    })
    .deserializeUsing<Sale>((account) => deserializeSale(account))
    .whereField(
      'discriminator',
      new Uint8Array([202, 64, 232, 171, 178, 172, 34, 183])
    );
}
