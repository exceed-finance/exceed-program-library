/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';

export type Receipt = Account<ReceiptAccountData>;

export type ReceiptAccountData = {
  discriminator: Uint8Array;
  buyer: PublicKey;
  sale: PublicKey;
  numTokensPurchased: bigint;
  numTokensRedeemed: bigint;
};

export type ReceiptAccountDataArgs = {
  buyer: PublicKey;
  sale: PublicKey;
  numTokensPurchased: number | bigint;
  numTokensRedeemed: number | bigint;
};

export function getReceiptAccountDataSerializer(): Serializer<
  ReceiptAccountDataArgs,
  ReceiptAccountData
> {
  return mapSerializer<ReceiptAccountDataArgs, any, ReceiptAccountData>(
    struct<ReceiptAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['buyer', publicKeySerializer()],
        ['sale', publicKeySerializer()],
        ['numTokensPurchased', u64()],
        ['numTokensRedeemed', u64()],
      ],
      { description: 'ReceiptAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([39, 154, 73, 106, 80, 102, 145, 153]),
    })
  ) as Serializer<ReceiptAccountDataArgs, ReceiptAccountData>;
}

export function deserializeReceipt(rawAccount: RpcAccount): Receipt {
  return deserializeAccount(rawAccount, getReceiptAccountDataSerializer());
}

export async function fetchReceipt(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Receipt> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'Receipt');
  return deserializeReceipt(maybeAccount);
}

export async function safeFetchReceipt(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Receipt | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeReceipt(maybeAccount) : null;
}

export async function fetchAllReceipt(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Receipt[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'Receipt');
    return deserializeReceipt(maybeAccount);
  });
}

export async function safeFetchAllReceipt(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Receipt[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeReceipt(maybeAccount as RpcAccount));
}

export function getReceiptGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'earlyPurchase',
    'EmzVeKtVHRc6AuzrJtowoJ5qfEkpK1R9WzAmcnjzgF1V'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      buyer: PublicKey;
      sale: PublicKey;
      numTokensPurchased: number | bigint;
      numTokensRedeemed: number | bigint;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      buyer: [8, publicKeySerializer()],
      sale: [40, publicKeySerializer()],
      numTokensPurchased: [72, u64()],
      numTokensRedeemed: [80, u64()],
    })
    .deserializeUsing<Receipt>((account) => deserializeReceipt(account))
    .whereField(
      'discriminator',
      new Uint8Array([39, 154, 73, 106, 80, 102, 145, 153])
    );
}

export function getReceiptSize(): number {
  return 88;
}
