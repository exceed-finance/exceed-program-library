/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type FundWithdrawalWindowInstructionAccounts = {
  accessControl?: PublicKey | Pda;
  pair: PublicKey | Pda;
  withdrawalWindow: PublicKey | Pda;
  depositAuthority: Signer;
  depositAuthorityBaseTokenAccount?: PublicKey | Pda;
  windowBaseTokenAccount?: PublicKey | Pda;
  baseTokenMint: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type FundWithdrawalWindowInstructionData = { discriminator: Uint8Array };

export type FundWithdrawalWindowInstructionDataArgs = {};

export function getFundWithdrawalWindowInstructionDataSerializer(): Serializer<
  FundWithdrawalWindowInstructionDataArgs,
  FundWithdrawalWindowInstructionData
> {
  return mapSerializer<
    FundWithdrawalWindowInstructionDataArgs,
    any,
    FundWithdrawalWindowInstructionData
  >(
    struct<FundWithdrawalWindowInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'FundWithdrawalWindowInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([231, 141, 11, 139, 32, 242, 97, 13]),
    })
  ) as Serializer<
    FundWithdrawalWindowInstructionDataArgs,
    FundWithdrawalWindowInstructionData
  >;
}

// Instruction.
export function fundWithdrawalWindow(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: FundWithdrawalWindowInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
    pair: { index: 1, isWritable: false as boolean, value: input.pair ?? null },
    withdrawalWindow: {
      index: 2,
      isWritable: true as boolean,
      value: input.withdrawalWindow ?? null,
    },
    depositAuthority: {
      index: 3,
      isWritable: true as boolean,
      value: input.depositAuthority ?? null,
    },
    depositAuthorityBaseTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.depositAuthorityBaseTokenAccount ?? null,
    },
    windowBaseTokenAccount: {
      index: 5,
      isWritable: true as boolean,
      value: input.windowBaseTokenAccount ?? null,
    },
    baseTokenMint: {
      index: 6,
      isWritable: false as boolean,
      value: input.baseTokenMint ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.depositAuthorityBaseTokenAccount.value) {
    resolvedAccounts.depositAuthorityBaseTokenAccount.value =
      context.eddsa.findPda(
        context.programs.getPublicKey(
          'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL',
          'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
        ),
        [
          publicKeySerializer().serialize(
            expectPublicKey(resolvedAccounts.depositAuthority.value)
          ),
          publicKeySerializer().serialize(
            expectPublicKey(resolvedAccounts.tokenProgram.value)
          ),
          publicKeySerializer().serialize(
            expectPublicKey(resolvedAccounts.baseTokenMint.value)
          ),
        ]
      );
  }
  if (!resolvedAccounts.windowBaseTokenAccount.value) {
    resolvedAccounts.windowBaseTokenAccount.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.withdrawalWindow.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenProgram.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.baseTokenMint.value)
        ),
      ]
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getFundWithdrawalWindowInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
