/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  i32,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u16,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  expectSome,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreatePairInstructionAccounts = {
  pairAuthority: Signer;
  /** The authority proof account */
  accessControl: PublicKey | Pda;
  baseTokenMint: PublicKey | Pda;
  pair?: PublicKey | Pda;
  pairBaseTokenAccount: PublicKey | Pda;
  lstMint?: PublicKey | Pda;
  lstFeeAccount: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  associatedTokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  rent?: PublicKey | Pda;
};

// Data.
export type CreatePairInstructionData = {
  discriminator: Uint8Array;
  symbol: string;
  intervalAprRate: bigint;
  secondsPerInterval: number;
  initialExchangeRate: bigint;
  depositCap: bigint;
  stakeFeeBps: number;
  swapFeeBps: number;
  withdrawFeeBps: number;
  minimumDeposit: bigint;
  pairType: number;
};

export type CreatePairInstructionDataArgs = {
  symbol: string;
  intervalAprRate: number | bigint;
  secondsPerInterval: number;
  initialExchangeRate: number | bigint;
  depositCap: number | bigint;
  stakeFeeBps: number;
  swapFeeBps: number;
  withdrawFeeBps: number;
  minimumDeposit: number | bigint;
  pairType: number;
};

export function getCreatePairInstructionDataSerializer(): Serializer<
  CreatePairInstructionDataArgs,
  CreatePairInstructionData
> {
  return mapSerializer<
    CreatePairInstructionDataArgs,
    any,
    CreatePairInstructionData
  >(
    struct<CreatePairInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['symbol', string()],
        ['intervalAprRate', u64()],
        ['secondsPerInterval', i32()],
        ['initialExchangeRate', u64()],
        ['depositCap', u64()],
        ['stakeFeeBps', u16()],
        ['swapFeeBps', u16()],
        ['withdrawFeeBps', u16()],
        ['minimumDeposit', u64()],
        ['pairType', u8()],
      ],
      { description: 'CreatePairInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([156, 190, 126, 151, 163, 62, 192, 220]),
    })
  ) as Serializer<CreatePairInstructionDataArgs, CreatePairInstructionData>;
}

// Args.
export type CreatePairInstructionArgs = CreatePairInstructionDataArgs;

// Instruction.
export function createPair(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: CreatePairInstructionAccounts & CreatePairInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    pairAuthority: {
      index: 0,
      isWritable: true as boolean,
      value: input.pairAuthority ?? null,
    },
    accessControl: {
      index: 1,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
    baseTokenMint: {
      index: 2,
      isWritable: false as boolean,
      value: input.baseTokenMint ?? null,
    },
    pair: { index: 3, isWritable: true as boolean, value: input.pair ?? null },
    pairBaseTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.pairBaseTokenAccount ?? null,
    },
    lstMint: {
      index: 5,
      isWritable: true as boolean,
      value: input.lstMint ?? null,
    },
    lstFeeAccount: {
      index: 6,
      isWritable: true as boolean,
      value: input.lstFeeAccount ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    rent: {
      index: 10,
      isWritable: false as boolean,
      value: input.rent ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreatePairInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.lstMint.value) {
    resolvedAccounts.lstMint.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([108, 115, 116, 95, 109, 105, 110, 116])
      ),
      string().serialize(expectSome(resolvedArgs.symbol)),
    ]);
  }
  if (!resolvedAccounts.pair.value) {
    resolvedAccounts.pair.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([112, 97, 105, 114])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.baseTokenMint.value)
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.lstMint.value)
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.associatedTokenProgram.value) {
    resolvedAccounts.associatedTokenProgram.value =
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      );
    resolvedAccounts.associatedTokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.rent.value) {
    resolvedAccounts.rent.value = context.programs.getPublicKey(
      'rent',
      'SysvarRent111111111111111111111111111111111'
    );
    resolvedAccounts.rent.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreatePairInstructionDataSerializer().serialize(
    resolvedArgs as CreatePairInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
