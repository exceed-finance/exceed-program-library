/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  mapSerializer,
  option,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateWhitelistInstructionAccounts = {
  accessControl?: PublicKey | Pda;
  authority?: Signer;
};

// Data.
export type UpdateWhitelistInstructionData = {
  discriminator: Uint8Array;
  merkleRoot: Option<Uint8Array>;
  enableWhitelist: Option<boolean>;
};

export type UpdateWhitelistInstructionDataArgs = {
  merkleRoot: OptionOrNullable<Uint8Array>;
  enableWhitelist: OptionOrNullable<boolean>;
};

export function getUpdateWhitelistInstructionDataSerializer(): Serializer<
  UpdateWhitelistInstructionDataArgs,
  UpdateWhitelistInstructionData
> {
  return mapSerializer<
    UpdateWhitelistInstructionDataArgs,
    any,
    UpdateWhitelistInstructionData
  >(
    struct<UpdateWhitelistInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['merkleRoot', option(bytes({ size: 32 }))],
        ['enableWhitelist', option(bool())],
      ],
      { description: 'UpdateWhitelistInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([94, 198, 33, 20, 192, 97, 44, 59]),
    })
  ) as Serializer<
    UpdateWhitelistInstructionDataArgs,
    UpdateWhitelistInstructionData
  >;
}

// Args.
export type UpdateWhitelistInstructionArgs = UpdateWhitelistInstructionDataArgs;

// Instruction.
export function updateWhitelist(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdateWhitelistInstructionAccounts & UpdateWhitelistInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateWhitelistInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateWhitelistInstructionDataSerializer().serialize(
    resolvedArgs as UpdateWhitelistInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
