/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type SealProgramInstructionAccounts = {
  /** The guardian that must sign */
  guardian: Signer;
  accessControl?: PublicKey | Pda;
};

// Data.
export type SealProgramInstructionData = { discriminator: Uint8Array };

export type SealProgramInstructionDataArgs = {};

export function getSealProgramInstructionDataSerializer(): Serializer<
  SealProgramInstructionDataArgs,
  SealProgramInstructionData
> {
  return mapSerializer<
    SealProgramInstructionDataArgs,
    any,
    SealProgramInstructionData
  >(
    struct<SealProgramInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'SealProgramInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([3, 61, 188, 97, 28, 118, 223, 207]),
    })
  ) as Serializer<SealProgramInstructionDataArgs, SealProgramInstructionData>;
}

// Instruction.
export function sealProgram(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: SealProgramInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );

  // Accounts.
  const resolvedAccounts = {
    guardian: {
      index: 0,
      isWritable: false as boolean,
      value: input.guardian ?? null,
    },
    accessControl: {
      index: 1,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSealProgramInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
