/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RestakeExpiredWithdrawInstructionAccounts = {
  accessControl?: PublicKey | Pda;
  pair: PublicKey | Pda;
  withdrawalWindow: PublicKey | Pda;
  withdrawalRequest: PublicKey | Pda;
  rentReceiver: PublicKey | Pda;
  windowAuthority: Signer;
  stakerLstTokenAccount: PublicKey | Pda;
  pairBaseTokenAccount: PublicKey | Pda;
  windowBaseTokenAccount: PublicKey | Pda;
  windowLstAccount: PublicKey | Pda;
  lstMint: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type RestakeExpiredWithdrawInstructionData = {
  discriminator: Uint8Array;
};

export type RestakeExpiredWithdrawInstructionDataArgs = {};

export function getRestakeExpiredWithdrawInstructionDataSerializer(): Serializer<
  RestakeExpiredWithdrawInstructionDataArgs,
  RestakeExpiredWithdrawInstructionData
> {
  return mapSerializer<
    RestakeExpiredWithdrawInstructionDataArgs,
    any,
    RestakeExpiredWithdrawInstructionData
  >(
    struct<RestakeExpiredWithdrawInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'RestakeExpiredWithdrawInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([40, 200, 38, 89, 138, 33, 77, 91]),
    })
  ) as Serializer<
    RestakeExpiredWithdrawInstructionDataArgs,
    RestakeExpiredWithdrawInstructionData
  >;
}

// Instruction.
export function restakeExpiredWithdraw(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: RestakeExpiredWithdrawInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
    pair: { index: 1, isWritable: false as boolean, value: input.pair ?? null },
    withdrawalWindow: {
      index: 2,
      isWritable: true as boolean,
      value: input.withdrawalWindow ?? null,
    },
    withdrawalRequest: {
      index: 3,
      isWritable: true as boolean,
      value: input.withdrawalRequest ?? null,
    },
    rentReceiver: {
      index: 4,
      isWritable: true as boolean,
      value: input.rentReceiver ?? null,
    },
    windowAuthority: {
      index: 5,
      isWritable: true as boolean,
      value: input.windowAuthority ?? null,
    },
    stakerLstTokenAccount: {
      index: 6,
      isWritable: true as boolean,
      value: input.stakerLstTokenAccount ?? null,
    },
    pairBaseTokenAccount: {
      index: 7,
      isWritable: true as boolean,
      value: input.pairBaseTokenAccount ?? null,
    },
    windowBaseTokenAccount: {
      index: 8,
      isWritable: true as boolean,
      value: input.windowBaseTokenAccount ?? null,
    },
    windowLstAccount: {
      index: 9,
      isWritable: true as boolean,
      value: input.windowLstAccount ?? null,
    },
    lstMint: {
      index: 10,
      isWritable: true as boolean,
      value: input.lstMint ?? null,
    },
    tokenProgram: {
      index: 11,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRestakeExpiredWithdrawInstructionDataSerializer().serialize(
    {}
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
