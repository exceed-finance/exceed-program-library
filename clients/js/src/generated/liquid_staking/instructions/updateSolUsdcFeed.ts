/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateSolUsdcFeedInstructionAccounts = {
  accessControl?: PublicKey | Pda;
  authority?: Signer;
};

// Data.
export type UpdateSolUsdcFeedInstructionData = {
  discriminator: Uint8Array;
  newFeedId: Uint8Array;
};

export type UpdateSolUsdcFeedInstructionDataArgs = { newFeedId: Uint8Array };

export function getUpdateSolUsdcFeedInstructionDataSerializer(): Serializer<
  UpdateSolUsdcFeedInstructionDataArgs,
  UpdateSolUsdcFeedInstructionData
> {
  return mapSerializer<
    UpdateSolUsdcFeedInstructionDataArgs,
    any,
    UpdateSolUsdcFeedInstructionData
  >(
    struct<UpdateSolUsdcFeedInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['newFeedId', bytes({ size: 32 })],
      ],
      { description: 'UpdateSolUsdcFeedInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([163, 230, 215, 217, 74, 137, 74, 1]),
    })
  ) as Serializer<
    UpdateSolUsdcFeedInstructionDataArgs,
    UpdateSolUsdcFeedInstructionData
  >;
}

// Args.
export type UpdateSolUsdcFeedInstructionArgs =
  UpdateSolUsdcFeedInstructionDataArgs;

// Instruction.
export function updateSolUsdcFeed(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdateSolUsdcFeedInstructionAccounts & UpdateSolUsdcFeedInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateSolUsdcFeedInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateSolUsdcFeedInstructionDataSerializer().serialize(
    resolvedArgs as UpdateSolUsdcFeedInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
