/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateAccessControlInstructionAccounts = {
  payer?: Signer;
  accessControl?: PublicKey | Pda;
  /** The admin that must sign */
  admin: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateAccessControlInstructionData = {
  discriminator: Uint8Array;
  vaultAuthority: PublicKey;
  windowAuthority: PublicKey;
  depositAuthority: PublicKey;
  pairAuthority: PublicKey;
  unsealAuthority: PublicKey;
  accessAuthority: PublicKey;
};

export type CreateAccessControlInstructionDataArgs = {
  vaultAuthority: PublicKey;
  windowAuthority: PublicKey;
  depositAuthority: PublicKey;
  pairAuthority: PublicKey;
  unsealAuthority: PublicKey;
  accessAuthority: PublicKey;
};

export function getCreateAccessControlInstructionDataSerializer(): Serializer<
  CreateAccessControlInstructionDataArgs,
  CreateAccessControlInstructionData
> {
  return mapSerializer<
    CreateAccessControlInstructionDataArgs,
    any,
    CreateAccessControlInstructionData
  >(
    struct<CreateAccessControlInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['vaultAuthority', publicKeySerializer()],
        ['windowAuthority', publicKeySerializer()],
        ['depositAuthority', publicKeySerializer()],
        ['pairAuthority', publicKeySerializer()],
        ['unsealAuthority', publicKeySerializer()],
        ['accessAuthority', publicKeySerializer()],
      ],
      { description: 'CreateAccessControlInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([83, 239, 186, 78, 109, 109, 153, 216]),
    })
  ) as Serializer<
    CreateAccessControlInstructionDataArgs,
    CreateAccessControlInstructionData
  >;
}

// Args.
export type CreateAccessControlInstructionArgs =
  CreateAccessControlInstructionDataArgs;

// Instruction.
export function createAccessControl(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: CreateAccessControlInstructionAccounts &
    CreateAccessControlInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    accessControl: {
      index: 1,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
    admin: {
      index: 2,
      isWritable: false as boolean,
      value: input.admin ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateAccessControlInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateAccessControlInstructionDataSerializer().serialize(
    resolvedArgs as CreateAccessControlInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
