/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  option,
  struct,
  u16,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdatePairLimitsInstructionAccounts = {
  pair: PublicKey | Pda;
  authority?: Signer;
  accessControl?: PublicKey | Pda;
};

// Data.
export type UpdatePairLimitsInstructionData = {
  discriminator: Uint8Array;
  depositCap: Option<bigint>;
  minimumDeposit: Option<bigint>;
  stakeFeeBps: Option<number>;
  swapFeeBps: Option<number>;
  withdrawFeeBps: Option<number>;
};

export type UpdatePairLimitsInstructionDataArgs = {
  depositCap: OptionOrNullable<number | bigint>;
  minimumDeposit: OptionOrNullable<number | bigint>;
  stakeFeeBps: OptionOrNullable<number>;
  swapFeeBps: OptionOrNullable<number>;
  withdrawFeeBps: OptionOrNullable<number>;
};

export function getUpdatePairLimitsInstructionDataSerializer(): Serializer<
  UpdatePairLimitsInstructionDataArgs,
  UpdatePairLimitsInstructionData
> {
  return mapSerializer<
    UpdatePairLimitsInstructionDataArgs,
    any,
    UpdatePairLimitsInstructionData
  >(
    struct<UpdatePairLimitsInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['depositCap', option(u64())],
        ['minimumDeposit', option(u64())],
        ['stakeFeeBps', option(u16())],
        ['swapFeeBps', option(u16())],
        ['withdrawFeeBps', option(u16())],
      ],
      { description: 'UpdatePairLimitsInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([229, 193, 200, 69, 199, 52, 189, 87]),
    })
  ) as Serializer<
    UpdatePairLimitsInstructionDataArgs,
    UpdatePairLimitsInstructionData
  >;
}

// Args.
export type UpdatePairLimitsInstructionArgs =
  UpdatePairLimitsInstructionDataArgs;

// Instruction.
export function updatePairLimits(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdatePairLimitsInstructionAccounts & UpdatePairLimitsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    pair: { index: 0, isWritable: true as boolean, value: input.pair ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    accessControl: {
      index: 2,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdatePairLimitsInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdatePairLimitsInstructionDataSerializer().serialize(
    resolvedArgs as UpdatePairLimitsInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
