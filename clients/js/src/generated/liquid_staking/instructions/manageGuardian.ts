/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  GuardianOperation,
  GuardianOperationArgs,
  getGuardianOperationSerializer,
} from '../types';

// Accounts.
export type ManageGuardianInstructionAccounts = {
  unsealAuthority: Signer;
  accessControl?: PublicKey | Pda;
};

// Data.
export type ManageGuardianInstructionData = {
  discriminator: Uint8Array;
  guardian: PublicKey;
  operation: GuardianOperation;
};

export type ManageGuardianInstructionDataArgs = {
  guardian: PublicKey;
  operation: GuardianOperationArgs;
};

export function getManageGuardianInstructionDataSerializer(): Serializer<
  ManageGuardianInstructionDataArgs,
  ManageGuardianInstructionData
> {
  return mapSerializer<
    ManageGuardianInstructionDataArgs,
    any,
    ManageGuardianInstructionData
  >(
    struct<ManageGuardianInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['guardian', publicKeySerializer()],
        ['operation', getGuardianOperationSerializer()],
      ],
      { description: 'ManageGuardianInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([229, 240, 143, 54, 137, 49, 201, 209]),
    })
  ) as Serializer<
    ManageGuardianInstructionDataArgs,
    ManageGuardianInstructionData
  >;
}

// Args.
export type ManageGuardianInstructionArgs = ManageGuardianInstructionDataArgs;

// Instruction.
export function manageGuardian(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: ManageGuardianInstructionAccounts & ManageGuardianInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    unsealAuthority: {
      index: 0,
      isWritable: false as boolean,
      value: input.unsealAuthority ?? null,
    },
    accessControl: {
      index: 1,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ManageGuardianInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getManageGuardianInstructionDataSerializer().serialize(
    resolvedArgs as ManageGuardianInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
