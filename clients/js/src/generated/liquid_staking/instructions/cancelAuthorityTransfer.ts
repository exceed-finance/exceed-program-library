/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AuthorityType,
  AuthorityTypeArgs,
  getAuthorityTypeSerializer,
} from '../types';

// Accounts.
export type CancelAuthorityTransferInstructionAccounts = {
  accessControl: PublicKey | Pda;
  /** The current authority that must sign */
  currentAuthority: Signer;
};

// Data.
export type CancelAuthorityTransferInstructionData = {
  discriminator: Uint8Array;
  authorityType: AuthorityType;
};

export type CancelAuthorityTransferInstructionDataArgs = {
  authorityType: AuthorityTypeArgs;
};

export function getCancelAuthorityTransferInstructionDataSerializer(): Serializer<
  CancelAuthorityTransferInstructionDataArgs,
  CancelAuthorityTransferInstructionData
> {
  return mapSerializer<
    CancelAuthorityTransferInstructionDataArgs,
    any,
    CancelAuthorityTransferInstructionData
  >(
    struct<CancelAuthorityTransferInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['authorityType', getAuthorityTypeSerializer()],
      ],
      { description: 'CancelAuthorityTransferInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([94, 131, 125, 184, 183, 24, 125, 229]),
    })
  ) as Serializer<
    CancelAuthorityTransferInstructionDataArgs,
    CancelAuthorityTransferInstructionData
  >;
}

// Args.
export type CancelAuthorityTransferInstructionArgs =
  CancelAuthorityTransferInstructionDataArgs;

// Instruction.
export function cancelAuthorityTransfer(
  context: Pick<Context, 'programs'>,
  input: CancelAuthorityTransferInstructionAccounts &
    CancelAuthorityTransferInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
    currentAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.currentAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CancelAuthorityTransferInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCancelAuthorityTransferInstructionDataSerializer().serialize(
    resolvedArgs as CancelAuthorityTransferInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
