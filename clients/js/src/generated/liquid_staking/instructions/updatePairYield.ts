/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdatePairYieldInstructionAccounts = {
  pair: PublicKey | Pda;
  authority?: Signer;
  accessControl?: PublicKey | Pda;
};

// Data.
export type UpdatePairYieldInstructionData = {
  discriminator: Uint8Array;
  intervalAprRate: bigint;
};

export type UpdatePairYieldInstructionDataArgs = {
  intervalAprRate: number | bigint;
};

export function getUpdatePairYieldInstructionDataSerializer(): Serializer<
  UpdatePairYieldInstructionDataArgs,
  UpdatePairYieldInstructionData
> {
  return mapSerializer<
    UpdatePairYieldInstructionDataArgs,
    any,
    UpdatePairYieldInstructionData
  >(
    struct<UpdatePairYieldInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['intervalAprRate', u64()],
      ],
      { description: 'UpdatePairYieldInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([106, 132, 147, 158, 54, 40, 218, 16]),
    })
  ) as Serializer<
    UpdatePairYieldInstructionDataArgs,
    UpdatePairYieldInstructionData
  >;
}

// Args.
export type UpdatePairYieldInstructionArgs = UpdatePairYieldInstructionDataArgs;

// Instruction.
export function updatePairYield(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdatePairYieldInstructionAccounts & UpdatePairYieldInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );

  // Accounts.
  const resolvedAccounts = {
    pair: { index: 0, isWritable: true as boolean, value: input.pair ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    accessControl: {
      index: 2,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdatePairYieldInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdatePairYieldInstructionDataSerializer().serialize(
    resolvedArgs as UpdatePairYieldInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
