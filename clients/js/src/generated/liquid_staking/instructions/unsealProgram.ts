/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UnsealProgramInstructionAccounts = {
  unsealAuthority: Signer;
  accessControl: PublicKey | Pda;
};

// Data.
export type UnsealProgramInstructionData = { discriminator: Uint8Array };

export type UnsealProgramInstructionDataArgs = {};

export function getUnsealProgramInstructionDataSerializer(): Serializer<
  UnsealProgramInstructionDataArgs,
  UnsealProgramInstructionData
> {
  return mapSerializer<
    UnsealProgramInstructionDataArgs,
    any,
    UnsealProgramInstructionData
  >(
    struct<UnsealProgramInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'UnsealProgramInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([183, 82, 151, 155, 126, 84, 192, 135]),
    })
  ) as Serializer<
    UnsealProgramInstructionDataArgs,
    UnsealProgramInstructionData
  >;
}

// Instruction.
export function unsealProgram(
  context: Pick<Context, 'programs'>,
  input: UnsealProgramInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    unsealAuthority: {
      index: 0,
      isWritable: false as boolean,
      value: input.unsealAuthority ?? null,
    },
    accessControl: {
      index: 1,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUnsealProgramInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
