/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AuthorityType,
  AuthorityTypeArgs,
  getAuthorityTypeSerializer,
} from '../types';

// Accounts.
export type InitiateAuthorityTransferInstructionAccounts = {
  accessControl?: PublicKey | Pda;
  /** The current authority that must sign */
  currentAuthority: Signer;
};

// Data.
export type InitiateAuthorityTransferInstructionData = {
  discriminator: Uint8Array;
  authorityType: AuthorityType;
  newAuthority: PublicKey;
};

export type InitiateAuthorityTransferInstructionDataArgs = {
  authorityType: AuthorityTypeArgs;
  newAuthority: PublicKey;
};

export function getInitiateAuthorityTransferInstructionDataSerializer(): Serializer<
  InitiateAuthorityTransferInstructionDataArgs,
  InitiateAuthorityTransferInstructionData
> {
  return mapSerializer<
    InitiateAuthorityTransferInstructionDataArgs,
    any,
    InitiateAuthorityTransferInstructionData
  >(
    struct<InitiateAuthorityTransferInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['authorityType', getAuthorityTypeSerializer()],
        ['newAuthority', publicKeySerializer()],
      ],
      { description: 'InitiateAuthorityTransferInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([210, 43, 101, 215, 119, 140, 106, 218]),
    })
  ) as Serializer<
    InitiateAuthorityTransferInstructionDataArgs,
    InitiateAuthorityTransferInstructionData
  >;
}

// Args.
export type InitiateAuthorityTransferInstructionArgs =
  InitiateAuthorityTransferInstructionDataArgs;

// Instruction.
export function initiateAuthorityTransfer(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: InitiateAuthorityTransferInstructionAccounts &
    InitiateAuthorityTransferInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
    currentAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.currentAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitiateAuthorityTransferInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitiateAuthorityTransferInstructionDataSerializer().serialize(
      resolvedArgs as InitiateAuthorityTransferInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
