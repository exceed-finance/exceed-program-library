/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AuthorityType,
  AuthorityTypeArgs,
  getAuthorityTypeSerializer,
} from '../types';

// Accounts.
export type AcceptAuthorityTransferInstructionAccounts = {
  accessControl: PublicKey | Pda;
  /** The new authority that must sign */
  newAuthority: Signer;
};

// Data.
export type AcceptAuthorityTransferInstructionData = {
  discriminator: Uint8Array;
  authorityType: AuthorityType;
};

export type AcceptAuthorityTransferInstructionDataArgs = {
  authorityType: AuthorityTypeArgs;
};

export function getAcceptAuthorityTransferInstructionDataSerializer(): Serializer<
  AcceptAuthorityTransferInstructionDataArgs,
  AcceptAuthorityTransferInstructionData
> {
  return mapSerializer<
    AcceptAuthorityTransferInstructionDataArgs,
    any,
    AcceptAuthorityTransferInstructionData
  >(
    struct<AcceptAuthorityTransferInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['authorityType', getAuthorityTypeSerializer()],
      ],
      { description: 'AcceptAuthorityTransferInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([239, 248, 177, 2, 206, 97, 46, 255]),
    })
  ) as Serializer<
    AcceptAuthorityTransferInstructionDataArgs,
    AcceptAuthorityTransferInstructionData
  >;
}

// Args.
export type AcceptAuthorityTransferInstructionArgs =
  AcceptAuthorityTransferInstructionDataArgs;

// Instruction.
export function acceptAuthorityTransfer(
  context: Pick<Context, 'programs'>,
  input: AcceptAuthorityTransferInstructionAccounts &
    AcceptAuthorityTransferInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );

  // Accounts.
  const resolvedAccounts = {
    accessControl: {
      index: 0,
      isWritable: true as boolean,
      value: input.accessControl ?? null,
    },
    newAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.newAuthority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AcceptAuthorityTransferInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAcceptAuthorityTransferInstructionDataSerializer().serialize(
    resolvedArgs as AcceptAuthorityTransferInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
