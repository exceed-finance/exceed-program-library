/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type VaultWithdrawInstructionAccounts = {
  pair: PublicKey | Pda;
  authority?: Signer;
  accessControl?: PublicKey | Pda;
  pairBaseTokenAccount?: PublicKey | Pda;
  authorityTokenAccount?: PublicKey | Pda;
  baseTokenMint: PublicKey | Pda;
  associatedTokenProgram?: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type VaultWithdrawInstructionData = {
  discriminator: Uint8Array;
  amount: bigint;
};

export type VaultWithdrawInstructionDataArgs = { amount: number | bigint };

export function getVaultWithdrawInstructionDataSerializer(): Serializer<
  VaultWithdrawInstructionDataArgs,
  VaultWithdrawInstructionData
> {
  return mapSerializer<
    VaultWithdrawInstructionDataArgs,
    any,
    VaultWithdrawInstructionData
  >(
    struct<VaultWithdrawInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amount', u64()],
      ],
      { description: 'VaultWithdrawInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([98, 28, 187, 98, 87, 69, 46, 64]),
    })
  ) as Serializer<
    VaultWithdrawInstructionDataArgs,
    VaultWithdrawInstructionData
  >;
}

// Args.
export type VaultWithdrawInstructionArgs = VaultWithdrawInstructionDataArgs;

// Instruction.
export function vaultWithdraw(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: VaultWithdrawInstructionAccounts & VaultWithdrawInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );

  // Accounts.
  const resolvedAccounts = {
    pair: { index: 0, isWritable: false as boolean, value: input.pair ?? null },
    authority: {
      index: 1,
      isWritable: true as boolean,
      value: input.authority ?? null,
    },
    accessControl: {
      index: 2,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
    pairBaseTokenAccount: {
      index: 3,
      isWritable: true as boolean,
      value: input.pairBaseTokenAccount ?? null,
    },
    authorityTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.authorityTokenAccount ?? null,
    },
    baseTokenMint: {
      index: 5,
      isWritable: false as boolean,
      value: input.baseTokenMint ?? null,
    },
    associatedTokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: VaultWithdrawInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.pairBaseTokenAccount.value) {
    resolvedAccounts.pairBaseTokenAccount.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.pair.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenProgram.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.baseTokenMint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.authorityTokenAccount.value) {
    resolvedAccounts.authorityTokenAccount.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.authority.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenProgram.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.baseTokenMint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.associatedTokenProgram.value) {
    resolvedAccounts.associatedTokenProgram.value =
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      );
    resolvedAccounts.associatedTokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getVaultWithdrawInstructionDataSerializer().serialize(
    resolvedArgs as VaultWithdrawInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
