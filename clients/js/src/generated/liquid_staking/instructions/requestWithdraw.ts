/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RequestWithdrawInstructionAccounts = {
  pair: PublicKey | Pda;
  withdrawalWindow: PublicKey | Pda;
  withdrawalRequest?: PublicKey | Pda;
  staker: Signer;
  stakerLstAccount: PublicKey | Pda;
  windowLstAccount: PublicKey | Pda;
  lstMint: PublicKey | Pda;
  accessControl?: PublicKey | Pda;
  priceFeed: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RequestWithdrawInstructionData = {
  discriminator: Uint8Array;
  amount: bigint;
  merkleProof: Option<Array<Uint8Array>>;
};

export type RequestWithdrawInstructionDataArgs = {
  amount: number | bigint;
  merkleProof: OptionOrNullable<Array<Uint8Array>>;
};

export function getRequestWithdrawInstructionDataSerializer(): Serializer<
  RequestWithdrawInstructionDataArgs,
  RequestWithdrawInstructionData
> {
  return mapSerializer<
    RequestWithdrawInstructionDataArgs,
    any,
    RequestWithdrawInstructionData
  >(
    struct<RequestWithdrawInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amount', u64()],
        ['merkleProof', option(array(bytes({ size: 32 })))],
      ],
      { description: 'RequestWithdrawInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([137, 95, 187, 96, 250, 138, 31, 182]),
    })
  ) as Serializer<
    RequestWithdrawInstructionDataArgs,
    RequestWithdrawInstructionData
  >;
}

// Args.
export type RequestWithdrawInstructionArgs = RequestWithdrawInstructionDataArgs;

// Instruction.
export function requestWithdraw(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: RequestWithdrawInstructionAccounts & RequestWithdrawInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'par1tyqusak2f2DXg9RHv78SVHNWXkJLSbtJZQSuWjV'
  );

  // Accounts.
  const resolvedAccounts = {
    pair: { index: 0, isWritable: true as boolean, value: input.pair ?? null },
    withdrawalWindow: {
      index: 1,
      isWritable: true as boolean,
      value: input.withdrawalWindow ?? null,
    },
    withdrawalRequest: {
      index: 2,
      isWritable: true as boolean,
      value: input.withdrawalRequest ?? null,
    },
    staker: {
      index: 3,
      isWritable: true as boolean,
      value: input.staker ?? null,
    },
    stakerLstAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.stakerLstAccount ?? null,
    },
    windowLstAccount: {
      index: 5,
      isWritable: true as boolean,
      value: input.windowLstAccount ?? null,
    },
    lstMint: {
      index: 6,
      isWritable: true as boolean,
      value: input.lstMint ?? null,
    },
    accessControl: {
      index: 7,
      isWritable: false as boolean,
      value: input.accessControl ?? null,
    },
    priceFeed: {
      index: 8,
      isWritable: false as boolean,
      value: input.priceFeed ?? null,
    },
    tokenProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RequestWithdrawInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.withdrawalRequest.value) {
    resolvedAccounts.withdrawalRequest.value = context.eddsa.findPda(
      programId,
      [
        bytes().serialize(
          new Uint8Array([
            119, 105, 116, 104, 100, 114, 97, 119, 97, 108, 95, 114, 101, 113,
            117, 101, 115, 116,
          ])
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.withdrawalWindow.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.staker.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.accessControl.value) {
    resolvedAccounts.accessControl.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          97, 99, 99, 101, 115, 115, 95, 99, 111, 110, 116, 114, 111, 108,
        ])
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRequestWithdrawInstructionDataSerializer().serialize(
    resolvedArgs as RequestWithdrawInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
