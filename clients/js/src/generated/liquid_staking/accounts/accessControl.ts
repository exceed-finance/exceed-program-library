/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  bytes,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type AccessControl = Account<AccessControlAccountData>;

export type AccessControlAccountData = {
  discriminator: Uint8Array;
  bump: number;
  merkleRoot: Uint8Array;
  isWhitelistEnabled: boolean;
  vaultAuthority: PublicKey;
  windowAuthority: PublicKey;
  depositAuthority: PublicKey;
  pairAuthority: PublicKey;
  unsealAuthority: PublicKey;
  accessAuthority: PublicKey;
  solUsdcFeedId: Uint8Array;
  guardians: Array<Option<PublicKey>>;
  isSealed: boolean;
  pendingVaultAuthority: Option<PublicKey>;
  pendingWindowAuthority: Option<PublicKey>;
  pendingDepositAuthority: Option<PublicKey>;
  pendingPairAuthority: Option<PublicKey>;
  pendingUnsealAuthority: Option<PublicKey>;
  pendingAccessAuthority: Option<PublicKey>;
  navAuthority: PublicKey;
  pendingNavAuthority: Option<PublicKey>;
};

export type AccessControlAccountDataArgs = {
  bump: number;
  merkleRoot: Uint8Array;
  isWhitelistEnabled: boolean;
  vaultAuthority: PublicKey;
  windowAuthority: PublicKey;
  depositAuthority: PublicKey;
  pairAuthority: PublicKey;
  unsealAuthority: PublicKey;
  accessAuthority: PublicKey;
  solUsdcFeedId: Uint8Array;
  guardians: Array<OptionOrNullable<PublicKey>>;
  isSealed: boolean;
  pendingVaultAuthority: OptionOrNullable<PublicKey>;
  pendingWindowAuthority: OptionOrNullable<PublicKey>;
  pendingDepositAuthority: OptionOrNullable<PublicKey>;
  pendingPairAuthority: OptionOrNullable<PublicKey>;
  pendingUnsealAuthority: OptionOrNullable<PublicKey>;
  pendingAccessAuthority: OptionOrNullable<PublicKey>;
  navAuthority: PublicKey;
  pendingNavAuthority: OptionOrNullable<PublicKey>;
};

export function getAccessControlAccountDataSerializer(): Serializer<
  AccessControlAccountDataArgs,
  AccessControlAccountData
> {
  return mapSerializer<
    AccessControlAccountDataArgs,
    any,
    AccessControlAccountData
  >(
    struct<AccessControlAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['bump', u8()],
        ['merkleRoot', bytes({ size: 32 })],
        ['isWhitelistEnabled', bool()],
        ['vaultAuthority', publicKeySerializer()],
        ['windowAuthority', publicKeySerializer()],
        ['depositAuthority', publicKeySerializer()],
        ['pairAuthority', publicKeySerializer()],
        ['unsealAuthority', publicKeySerializer()],
        ['accessAuthority', publicKeySerializer()],
        ['solUsdcFeedId', bytes({ size: 32 })],
        ['guardians', array(option(publicKeySerializer()), { size: 5 })],
        ['isSealed', bool()],
        ['pendingVaultAuthority', option(publicKeySerializer())],
        ['pendingWindowAuthority', option(publicKeySerializer())],
        ['pendingDepositAuthority', option(publicKeySerializer())],
        ['pendingPairAuthority', option(publicKeySerializer())],
        ['pendingUnsealAuthority', option(publicKeySerializer())],
        ['pendingAccessAuthority', option(publicKeySerializer())],
        ['navAuthority', publicKeySerializer()],
        ['pendingNavAuthority', option(publicKeySerializer())],
      ],
      { description: 'AccessControlAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([147, 81, 178, 92, 223, 66, 181, 132]),
    })
  ) as Serializer<AccessControlAccountDataArgs, AccessControlAccountData>;
}

export function deserializeAccessControl(
  rawAccount: RpcAccount
): AccessControl {
  return deserializeAccount(
    rawAccount,
    getAccessControlAccountDataSerializer()
  );
}

export async function fetchAccessControl(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AccessControl> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'AccessControl');
  return deserializeAccessControl(maybeAccount);
}

export async function safeFetchAccessControl(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AccessControl | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeAccessControl(maybeAccount) : null;
}

export async function fetchAllAccessControl(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AccessControl[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'AccessControl');
    return deserializeAccessControl(maybeAccount);
  });
}

export async function safeFetchAllAccessControl(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AccessControl[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeAccessControl(maybeAccount as RpcAccount)
    );
}

export function getAccessControlGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      bump: number;
      merkleRoot: Uint8Array;
      isWhitelistEnabled: boolean;
      vaultAuthority: PublicKey;
      windowAuthority: PublicKey;
      depositAuthority: PublicKey;
      pairAuthority: PublicKey;
      unsealAuthority: PublicKey;
      accessAuthority: PublicKey;
      solUsdcFeedId: Uint8Array;
      guardians: Array<OptionOrNullable<PublicKey>>;
      isSealed: boolean;
      pendingVaultAuthority: OptionOrNullable<PublicKey>;
      pendingWindowAuthority: OptionOrNullable<PublicKey>;
      pendingDepositAuthority: OptionOrNullable<PublicKey>;
      pendingPairAuthority: OptionOrNullable<PublicKey>;
      pendingUnsealAuthority: OptionOrNullable<PublicKey>;
      pendingAccessAuthority: OptionOrNullable<PublicKey>;
      navAuthority: PublicKey;
      pendingNavAuthority: OptionOrNullable<PublicKey>;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      bump: [8, u8()],
      merkleRoot: [9, bytes({ size: 32 })],
      isWhitelistEnabled: [41, bool()],
      vaultAuthority: [42, publicKeySerializer()],
      windowAuthority: [74, publicKeySerializer()],
      depositAuthority: [106, publicKeySerializer()],
      pairAuthority: [138, publicKeySerializer()],
      unsealAuthority: [170, publicKeySerializer()],
      accessAuthority: [202, publicKeySerializer()],
      solUsdcFeedId: [234, bytes({ size: 32 })],
      guardians: [266, array(option(publicKeySerializer()), { size: 5 })],
      isSealed: [null, bool()],
      pendingVaultAuthority: [null, option(publicKeySerializer())],
      pendingWindowAuthority: [null, option(publicKeySerializer())],
      pendingDepositAuthority: [null, option(publicKeySerializer())],
      pendingPairAuthority: [null, option(publicKeySerializer())],
      pendingUnsealAuthority: [null, option(publicKeySerializer())],
      pendingAccessAuthority: [null, option(publicKeySerializer())],
      navAuthority: [null, publicKeySerializer()],
      pendingNavAuthority: [null, option(publicKeySerializer())],
    })
    .deserializeUsing<AccessControl>((account) =>
      deserializeAccessControl(account)
    )
    .whereField(
      'discriminator',
      new Uint8Array([147, 81, 178, 92, 223, 66, 181, 132])
    );
}
