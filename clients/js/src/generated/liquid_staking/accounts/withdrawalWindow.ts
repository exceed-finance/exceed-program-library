/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  i64,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type WithdrawalWindow = Account<WithdrawalWindowAccountData>;

export type WithdrawalWindowAccountData = {
  discriminator: Uint8Array;
  pair: PublicKey;
  startTime: bigint;
  endTime: bigint;
  requestedWithdrawalAmount: bigint;
  totalLstBurned: bigint;
  maxWithdrawalAmount: bigint;
  earliestWithdrawalTime: bigint;
  expirationTime: bigint;
  baseTokenMint: PublicKey;
  baseTokenAccount: PublicKey;
  isFunded: boolean;
  bump: number;
  withdrawnAmount: bigint;
};

export type WithdrawalWindowAccountDataArgs = {
  pair: PublicKey;
  startTime: number | bigint;
  endTime: number | bigint;
  requestedWithdrawalAmount: number | bigint;
  totalLstBurned: number | bigint;
  maxWithdrawalAmount: number | bigint;
  earliestWithdrawalTime: number | bigint;
  expirationTime: number | bigint;
  baseTokenMint: PublicKey;
  baseTokenAccount: PublicKey;
  isFunded: boolean;
  bump: number;
  withdrawnAmount: number | bigint;
};

export function getWithdrawalWindowAccountDataSerializer(): Serializer<
  WithdrawalWindowAccountDataArgs,
  WithdrawalWindowAccountData
> {
  return mapSerializer<
    WithdrawalWindowAccountDataArgs,
    any,
    WithdrawalWindowAccountData
  >(
    struct<WithdrawalWindowAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['pair', publicKeySerializer()],
        ['startTime', i64()],
        ['endTime', i64()],
        ['requestedWithdrawalAmount', u64()],
        ['totalLstBurned', u64()],
        ['maxWithdrawalAmount', u64()],
        ['earliestWithdrawalTime', i64()],
        ['expirationTime', i64()],
        ['baseTokenMint', publicKeySerializer()],
        ['baseTokenAccount', publicKeySerializer()],
        ['isFunded', bool()],
        ['bump', u8()],
        ['withdrawnAmount', u64()],
      ],
      { description: 'WithdrawalWindowAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([15, 162, 80, 215, 210, 205, 94, 9]),
    })
  ) as Serializer<WithdrawalWindowAccountDataArgs, WithdrawalWindowAccountData>;
}

export function deserializeWithdrawalWindow(
  rawAccount: RpcAccount
): WithdrawalWindow {
  return deserializeAccount(
    rawAccount,
    getWithdrawalWindowAccountDataSerializer()
  );
}

export async function fetchWithdrawalWindow(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<WithdrawalWindow> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'WithdrawalWindow');
  return deserializeWithdrawalWindow(maybeAccount);
}

export async function safeFetchWithdrawalWindow(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<WithdrawalWindow | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeWithdrawalWindow(maybeAccount) : null;
}

export async function fetchAllWithdrawalWindow(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<WithdrawalWindow[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'WithdrawalWindow');
    return deserializeWithdrawalWindow(maybeAccount);
  });
}

export async function safeFetchAllWithdrawalWindow(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<WithdrawalWindow[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeWithdrawalWindow(maybeAccount as RpcAccount)
    );
}

export function getWithdrawalWindowGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'liquidStaking',
    'GTmavJc15q4TowiFmdzFU7VFsfkfahxiMXT9xfS9eeCo'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      pair: PublicKey;
      startTime: number | bigint;
      endTime: number | bigint;
      requestedWithdrawalAmount: number | bigint;
      totalLstBurned: number | bigint;
      maxWithdrawalAmount: number | bigint;
      earliestWithdrawalTime: number | bigint;
      expirationTime: number | bigint;
      baseTokenMint: PublicKey;
      baseTokenAccount: PublicKey;
      isFunded: boolean;
      bump: number;
      withdrawnAmount: number | bigint;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      pair: [8, publicKeySerializer()],
      startTime: [40, i64()],
      endTime: [48, i64()],
      requestedWithdrawalAmount: [56, u64()],
      totalLstBurned: [64, u64()],
      maxWithdrawalAmount: [72, u64()],
      earliestWithdrawalTime: [80, i64()],
      expirationTime: [88, i64()],
      baseTokenMint: [96, publicKeySerializer()],
      baseTokenAccount: [128, publicKeySerializer()],
      isFunded: [160, bool()],
      bump: [161, u8()],
      withdrawnAmount: [162, u64()],
    })
    .deserializeUsing<WithdrawalWindow>((account) =>
      deserializeWithdrawalWindow(account)
    )
    .whereField(
      'discriminator',
      new Uint8Array([15, 162, 80, 215, 210, 205, 94, 9])
    );
}

export function getWithdrawalWindowSize(): number {
  return 170;
}
